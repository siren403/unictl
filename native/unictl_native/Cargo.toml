[package]
name = "unictl_native"
version = "0.1.0"
edition = "2024"

[dependencies]
tiny_http = "0.12"

[target.'cfg(target_os = "macos")'.dependencies]
core-foundation = "0.10"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true
strip = true
