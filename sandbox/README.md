# unictl Sandbox

This directory contains local Unity consumer projects used for smoke testing `unictl`
installation and editor integration flows.

Tracked files should stay limited to project source, `Packages/`, and
`ProjectSettings/`. Unity-generated local state such as `Library/`, `Temp/`,
`Logs/`, and `UserSettings/` is intentionally ignored.
