# unictl Codex Integration Source

이 디렉터리는 Codex용 thin wrapper source다.

원칙:

- 공통 사용 문서는 `unictl/docs/standalone/`에서 복사한다.
- 이 디렉터리에는 Codex-specific manifest와 workflow skill만 둔다.
- generated release artifact는 `codex-plugin-X.Y.Z.zip` 이름으로 조립된다.
