# unictl Claude Code Support Source

이 디렉터리는 Claude Code용 thin wrapper source다.

포함 범위:

- `.claude/agents`
- `.claude/rules`
- generated copy 대상 공통 문서
- install helper

공통 설명은 `unictl/docs/standalone/`를 source로 삼고, 여기서는 Claude-specific routing만 유지한다.
