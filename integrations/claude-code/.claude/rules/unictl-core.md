# unictl core rule

- Run `doctor` before assuming the Unity editor bridge is healthy.
- Prefer `command <tool>` over repo-specific shortcuts when using built-in `unictl` tools.
- Treat Codex and Claude support packs as wrappers around shared docs, not separate product definitions.
- Keep project-specific tools in the consumer repo with `[UnictlTool]`.
